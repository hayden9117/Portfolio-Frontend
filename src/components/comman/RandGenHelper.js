

export default function RandGenHelper (click) {
    let shotList = ["Musty flick", " 180 flick", "90 degree flick", "JZR flick"];
  
    if (click === true ){  
    for (let i = 0; i <= shotList.length; i++ ) {
        i = Math.floor(Math.random) 
        return shotList[i]
    }
}

}