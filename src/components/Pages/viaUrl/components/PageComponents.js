import { Box, List, ListItem, Typography } from "@mui/material";

export const LinkList = () => {
  return (
    <Box>
      {" "}
      <List></List>
    </Box>
  );
};

export const PageName = () => {
  return (
    <Box>
      <Typography />
    </Box>
  );
};
