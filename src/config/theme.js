import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#4dacff",
    },
    secondary: {
      main: "#f50057",
    },
    background: {
      default: "#1f3347",
      paper: "#52667a",
    },
  },
});
